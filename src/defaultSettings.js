const DEFAULT_DURATIONS = {
  pomodoro: 25 * 60,
  "short pause": 5 * 60,
  "long pause": 15 * 60,
};

const DEFAULT_THEME = {
  color: "var(--coral-color)",
  font: "var(--font-sans)",
};

export { DEFAULT_DURATIONS, DEFAULT_THEME };
